<!-- <div class="container"> -->
  <p id="notice"></p>
    <!-- <div class="row"> -->
	  <div class="col-xs-3">
	    <h2>User info</h2>


		<table class="table">
		  <tbody>
		    <tr><%= attachment_image_tag @user, :profile_image, format: 'jpeg',fallback: "no_image.jpg", class: "profile_img" %></tr>
			<tr>
				<th>name</th>
				<th><%= user.name %></th>
			</tr>
			<tr>
			   <th>introduction</th>
			   <th><%= user.introduction %></th>
			</tr>
		  </tbody>
		</table>

       <% if user.id == current_user.id %>

        <%= link_to "", edit_user_path(@user.id), class: "btn btn-default edit_user_#{@user.id} glyphicon glyphicon-wrench" %>

       <% end %>

		<h2> New book</h2>
		<%= form_for(book) do |f| %>

		<h4>Title</h4>
		<%= f.text_field :title, class: "book_title" %>

		<h4>Opinion</h4>
		<%= f.text_area :body, class: "book_body" %>
        
        <%= f.submit 'Create Book', class:"btn btn-primary col-xs-12" %>
		<% end %>
	  </div>
<!-- 	</div>
</div>
 -->